# 结果尾页设计说明

## 1. 页面定位

结果尾页用于承接用户完成 12 道题后的最终展示，目标是把“测验结果 + 食物视觉 + 解释文案”组合成一个可截图、可阅读、可继续重开的收尾页。

当前设计风格关键词：

- 纸张感
- 轻复古
- 手作感
- 移动端长页阅读
- 食物海报式结果展示

对应实现入口：

- 页面结构：`index.html`
- 样式：`css/style.css`
- 结果标题与内容组装：`js/main.js`

## 2. 页面结构

结果页 DOM 结构如下：

```html
<section id="screen-result" class="screen hidden">
  <div class="paper">
    <img id="result-food" alt="食物插画" />
    <div class="result-title">
      <span class="result-title-prefix">你是——</span>
      <span class="result-name-wrap">
        <svg id="result-name-art" class="result-name-art"></svg>
      </span>
    </div>
    <div id="result-sections" class="sections"></div>
    <button id="btn-restart" class="btn primary" type="button">重新熬一锅</button>
  </div>
</section>
```

模块顺序说明：

1. 食物结果图
2. 标题区
3. 正文解释区
4. 重开按钮

## 3. 页面容器参数

### 3.1 全局变量

定义位置：`css/style.css`

| 变量 | 值 | 作用 |
| --- | --- | --- |
| `--paper` | `#fbf7ef` | 纸张底色 |
| `--ink` | `#2a2a2a` | 主文字色 |
| `--shadow` | `rgba(22,22,22,.12)` | 大卡片阴影 |
| `--radius` | `20px` | 主卡片圆角 |
| `--result-img-filter` | `brightness(1.12) contrast(1.04) saturate(1.06)` | 结果图提亮和提纯 |

### 3.2 页面层级

结果页挂在绝对定位 UI 层之上，底下仍然保留 Canvas 背景。

关键布局参数：

- `#ui`：绝对定位铺满全屏
- 外层内边距：`var(--ui-pad-y) var(--ui-pad-x) var(--ui-pad-bottom)`
- `#screen-result`：宽度拉满，适配移动端全屏
- `#screen-result .paper`：纵向布局，支持整页滚动

### 3.3 主卡片 `.paper`

| 项目 | 参数 |
| --- | --- |
| 宽度 | `width: 100%` |
| 最大宽度 | `max-width: 100%` |
| 背景 | `rgba(251,247,239,.86)` |
| 圆角 | `20px` |
| 内边距 | `clamp(14px, 3.8vw, 18px) clamp(14px, 4vw, 18px)` |
| 阴影 | `0 14px 40px rgba(22,22,22,.12)` |
| 边框 | `1px solid rgba(42,42,42,.08)` |
| 背景模糊 | `backdrop-filter: blur(8px)` |

设计意图：

- 让整个结果区像一张半透明纸卡浮在背景上
- 保持温和，不做强对比商务卡片
- 在移动端有足够呼吸感，但不浪费纵向空间

## 4. 结果图设计

### 4.1 食物图 `#result-food`

| 项目 | 参数 |
| --- | --- |
| 宽度 | `100%` |
| 比例 | `1 / 1` |
| 背景 | `#fff` |
| 边框 | `1px solid rgba(42,42,42,.12)` |
| 圆角 | `0` |
| 阴影 | 无 |
| 填充方式 | `object-fit: contain` |
| 对齐方式 | `object-position: center` |
| 图像滤镜 | `var(--result-img-filter)` |

设计意图：

- 结果图区域像一张平整插画稿，不做圆角照片感
- 用纯白底承接每一道菜的 PNG 结果图
- 用轻微滤镜统一整体亮度和饱和度

## 5. 标题区设计

标题区由两部分组成：

- 前缀：`你是——`
- 菜名主标题：动态 SVG 手写标题

### 5.1 标题区容器 `.result-title`

| 项目 | 参数 |
| --- | --- |
| 上边距 | `12px` |
| 布局 | `flex` |
| 方向 | `column` |
| 对齐 | `align-items: flex-start` |
| 元素间距 | `6px` |

### 5.2 前缀标题 `.result-title-prefix`

这是黑体字，不参与手写效果。

| 项目 | 参数 |
| --- | --- |
| 字号 | `clamp(22px, 6.6vw, 30px)` |
| 行高 | `1.08` |
| 字距 | `0.4px` |
| 字重 | `900` |
| 颜色 | `rgba(22,22,22,.94)` |
| 字体栈 | `"Microsoft YaHei", "PingFang SC", "Helvetica Neue", Arial, sans-serif` |

设计意图：

- 用黑体保证信息起始语明确、稳定
- 和后面的菜名形成“规则标题 + 手写结果”的对比

### 5.3 菜名主标题容器

| 项目 | 参数 |
| --- | --- |
| 类名 | `.result-name-wrap` |
| 宽度 | `min(100%, 360px)` |
| 呈现方式 | 内部承载 `svg` |

### 5.4 菜名字体方案

当前不是普通 HTML 文本，而是动态生成 SVG 标题。

生成位置：`js/main.js`

核心参数：

| 项目 | 参数 |
| --- | --- |
| SVG 视口高度 | `132` |
| 基础字号 | `92` |
| 字距 | `3 字以内 12 / 5 字以内 8 / 更长 4` |
| 主字体栈 | `"STXingkai", "STKaiti", "KaiTi", "Kaiti SC", "FangSong", serif` |
| 主色 | `#b43a19` |
| 辅助色 | `#a03116` |

SVG 文字由两层叠加组成：

1. 主文字层
2. 偏移轮廓层

滤镜参数：

| 滤镜 | 参数 | 用途 |
| --- | --- | --- |
| `rough-fill` | `feTurbulence baseFrequency=0.85` + `feDisplacementMap scale=2.4` | 让主笔画像手写毛边 |
| `rough-edge` | `feTurbulence baseFrequency=0.95` + `feDisplacementMap scale=3.1` + `feMorphology radius=0.5` | 让外轮廓更粗糙、更像印在纸上的边缘 |

设计意图：

- 菜名必须区别于普通正文，像“结果海报的主标题”
- 不是简单换颜色，而是字形本身有笔触感和颗粒感
- 适配不同长度菜名，短词更松，长词自动收紧

## 6. 正文卡片区设计

正文区通过 `#result-sections` 动态生成，包含若干信息卡片。

### 6.1 区块容器 `.sections`

| 项目 | 参数 |
| --- | --- |
| 上边距 | `12px` |
| 布局 | 纵向 `flex` |
| 区块间距 | `12px` |
| 底部留白 | `12px` |

### 6.2 单个内容卡 `.block`

| 项目 | 参数 |
| --- | --- |
| 背景 | `rgba(251,247,239,.82)` |
| 边框 | `1px solid rgba(42,42,42,.12)` |
| 圆角 | `14px` |
| 内边距 | `12px 12px` |
| 阴影 | `0 6px 22px rgba(20,20,20,.05)` |

这就是你说的“画框”部分，当前采用的是轻边框圆角卡片，不是厚描边，也不是完全无边框。

设计意图：

- 和主卡片统一材质
- 用小卡片把结果信息拆成更好读的段落
- 保持层次，但不喧宾夺主

### 6.3 卡片标题 `.block-title`

| 项目 | 参数 |
| --- | --- |
| 显示方式 | `inline-flex` |
| 内边距 | `6px 10px` |
| 圆角 | `999px` |
| 背景 | `rgba(111,142,168,.14)` |
| 边框 | `1px solid rgba(111,142,168,.18)` |
| 字号 | `12px` |
| 字重 | `850` |
| 字距 | `2px` |
| 颜色 | `rgba(42,42,42,.76)` |

标题文案来源：

- 为什么是这道菜
- 内心戏
- 建议
- 如果原始文案没有标题，则默认使用“分析”

### 6.4 卡片正文 `.block-body`

| 项目 | 参数 |
| --- | --- |
| 上边距 | `8px` |
| 字号 | `clamp(14px, 3.6vw, 15px)` |
| 行高 | `1.8` |
| 字距 | `0.2px` |
| 颜色 | `rgba(42,42,42,.86)` |
| 换行 | `white-space: pre-wrap` |

设计意图：

- 保证移动端长段落的阅读舒适度
- 用较高行高减少压迫感

## 7. 尾部灰字区设计

这是正文最后一段的补充信息区，不放进卡片，而是独立放在卡片组后面。

组成如下：

1. 分隔线文字 `------`
2. 小标题 `灵魂剖析`
3. 灰色斜体正文

### 7.1 分隔符 `.soul-divider`

| 项目 | 参数 |
| --- | --- |
| 上边距 | `12px` |
| 对齐 | 居中 |
| 字号 | `12px` |
| 字距 | `4px` |
| 颜色 | `rgba(42,42,42,.35)` |

### 7.2 小标题 `.soul-title`

| 项目 | 参数 |
| --- | --- |
| 上边距 | `8px` |
| 字号 | `12px` |
| 字重 | `800` |
| 字距 | `2px` |
| 颜色 | `rgba(42,42,42,.52)` |

### 7.3 灰色斜体正文 `.soul-text`

| 项目 | 参数 |
| --- | --- |
| 上边距 | `6px` |
| 字号 | `clamp(12px, 3.1vw, 13px)` |
| 行高 | `1.7` |
| 颜色 | `rgba(42,42,42,.62)` |
| 字形 | `italic` |
| 换行 | `white-space: pre-wrap` |

设计意图：

- 和主说明内容拉开层级
- 像“尾注”或“心理侧写补充”
- 用灰色斜体降低打扰感，适合作为收束信息

## 8. 文案组装逻辑

结果页正文由 `js/main.js` 动态组装。

### 8.1 文案来源

每个菜品结果对象包含：

- `name`
- `reading`
- `soul`
- `image`

### 8.2 `reading` 的拆分规则

程序会识别以下标题行：

- `为什么是这道菜？`
- `内心戏：`
- `建议：`

识别到后分别生成一个 `.block` 卡片。

如果一开始没有识别到标题，则默认用：

- `分析`

作为首个卡片标题。

### 8.3 `soul` 的处理规则

尾部灰字区域会先去掉以下前缀：

- `【灵魂侧写】`
- `灵魂侧写：`

然后生成：

- `.soul-divider`
- `.soul-title`
- `.soul-text`

## 9. 交互与状态

### 9.1 页面滚动

结果页 `.paper` 支持纵向滚动：

- `overflow-y: auto`
- 隐藏滚动条

适合长文案、长图文在移动端完整阅读。

### 9.2 重开按钮

按钮文案：

- `重新熬一锅`

位置：

- 正文区后方

功能：

- 重新进入答题流程

## 10. 当前视觉结论

当前结果尾页设计可以概括为：

- 顶部是白底菜品图，像插画成品稿
- 中间是“黑体前缀 + 手写菜名”的结果标题
- 下方是多张浅纸感信息卡片
- 尾部补一段灰色斜体的“灵魂剖析”
- 最后用“重新熬一锅”完成闭环

这套设计适合：

- 移动端 H5
- 截图分享
- 轻情绪测试
- 插画和文字并重的结果页

## 11. 后续可继续优化的方向

如果下一轮要继续打磨，建议优先看这 5 个方向：

1. 菜名字体改成真正的外部字体文件，而不是 SVG 伪手写
2. 结果图区域增加轻纸纹或装订边，提高海报感
3. 每张内容卡加入小图标，增强信息分段识别
4. 尾部“灵魂剖析”加更明确的视觉分隔
5. 重开按钮改成更统一的结果页专属按钮风格
